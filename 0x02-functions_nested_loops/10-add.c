#include "main.h"

/**
 * add - Adds two numbers
 * @x: the first number to compute
 *
 * @y: the second number to compute
 *
 * Return: success result
 */

int add(int x, int y)
{
	return (x + y);
}
